# Your Hermes agent origin (the process that publishes slime).
# Same contract as the original NDS dashboard:
#   GET  {origin}/api/dashboard.json
#   GET  {origin}/api/deliverables
#   GET  {origin}/api/session/:id
#   WS   {origin}/ws
NEXT_PUBLIC_HERMES_ORIGIN=https://area-interval-possession-options.trycloudflare.com
